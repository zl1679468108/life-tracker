# T78 Web 真实表单 CRUD 冒烟记录

日期：2026-07-02  
环境：本地 Web PWA `http://localhost:3021` + 后端 `http://localhost:3020`，应用内浏览器真实登录态。

## 覆盖范围

| 流程 | 结果 | 证据 |
|---|---|---|
| 位置管理新增 | 先复现新增后重复回显；修复 `locationStore` upsert 后，新增 `QA 位置修复 04752` 仅回显一次 | `docs/qa/t78-real-crud-20260702/location-after-fix.png` |
| 模板管理 | 真实账号模板列表为空；当前模板管理页只支持查看、套用、删除，未提供独立新建模板入口 | 页面空态文案：从物品或待办详情页保存为模板 |
| 借用新增 | 选择真实物品 `QA 全字段物品 0701-F`，填写 `QA 借用人 63681`，提交后出现“借用记录已创建”Toast | `docs/qa/t78-real-crud-20260702/borrowings-after-return.png` |
| 借用归还 | 应用内确认弹窗可用；确认后借用列表计数从借出中 3 / 已归还 0 变为借出中 2 / 已归还 1 | `docs/qa/t78-real-crud-20260702/borrowings-after-return.png` |

## 本轮修复

- `frontend/stores/locationStore.ts`：新增/更新/删除位置补齐错误抛出，新增和 socket 创建事件统一按 `id` upsert，避免 REST 创建响应和实时事件同时到达时重复插入。
- `backend/src/borrowings/borrowings.service.ts`：创建借用记录前检查同一用户、同一物品是否已有 `borrowed` / `overdue` 记录，后端兜底阻止同一物品未归还时重复借出。

## 残留记录

- 位置页修复前样本 `QA 位置 0702-23952` 曾重复回显；刷新后真实后端仅保留单条，新修复样本已验证单条回显。
- 借用页修复前通过坐标重复提交产生了 3 条同物品借用记录；本轮已归还其中 1 条，并补了后端重复借出兜底。历史测试数据不影响功能验证，但后续可按需清理。
- 借用新增页直接 URL 进入时，成功后 `router.back()` 没有历史页面可返回，会停留在创建页；从借用管理页入口进入时有返回历史。该行为记录为低风险 Web 直达路径体验问题。
