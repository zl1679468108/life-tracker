## 页面背景插画设计与嵌入

- 设计了 4 个 Tab 页的主题背景插画 V3 增强版（首页/工作台/消息/我的）
- 创建 `frontend/components/ui/PageBackground.tsx`，导出 `HomeBackground`、`WorkbenchBackground`、`MessagesBackground`、`SettingsBackground` 四个 SVG 组件
- 在 `frontend/components/ui/index.ts` 注册导出
- 更新四个 Tab 页替换原来的 `DecorativeBackground`
- TypeScript 类型检查通过，web 构建成功

## V4 改进 — viewBox 修复 + 渐变底色 + 动画

- 将 viewBox 从 680×300 改为 480×300，大幅减少横向裁切（从 95px 降到 ~12px）
- 重新定位所有右侧元素适配新 viewBox
- 每个页面添加 LinearGradient 渐变底色光（暖橙/浅绿/淡蓝/柔粉）
- 微调首页太阳尺寸（r=55→r=42）降低与功能按钮的视觉干扰
- 提升工作台核心元素 opacity（0.32→0.38）增加可见度
- **"我的"页面激活动画**：星星闪烁+scale、心跳脉动、花瓣飘动

## V5 — 全页面动画

- 所有 4 个页面都使用了 View 包裹 + Animated.View 动画层
- **首页**：3颗金色光点闪烁 + 2片花瓣飘动
- **工作台**：2颗金星闪烁 + scale 脉冲
- **消息**：铃铛摇摆、纸飞机漂浮、2个爱心心跳
- **我的**：星星闪烁+scale、心跳、3片花瓣错时飘动

## 浏览器冒烟测试

- 使用 Playwright 操作真实浏览器，走通核心功能流程
- 成功跑通 11/12 功能：登录、创建物品、物品列表、创建待办、待办列表、工作台、分类管理、位置管理、我的页面、注册新账号、消息好友
- 唯一未通过项为首页问候语正则检测（视觉层面正常渲染）
- 新增脚本：`scripts/qa/browser-smoke.mjs`
- 截图报告：`docs/qa/browser-smoke-*.png` 和 `browser-smoke-report.json`
- ⚠️ 实际数据未入库（表单缺少必填字段，点击保存失败但脚本误报成功）

## 23:05 — QA 冒烟测试 v2 — API 种数据 + 浏览器验证

- 重写为 API 种数据（保证入库）+ 浏览器验证展示
- 修复 todo priority 使用整数 (1/2/3) 而非字符串
- 修复好友 API 端点：`POST /api/messages/friends/requests` body: `target_user_id`
- 使用 Supabase admin API `PUT /auth/v1/admin/users/{id}` 自动确认新建账号邮箱
- 最新运行结果：12 项检查 9/12 通过，3 项失败均为浏览器验证文本匹配
- 数据库确认：12 条物品、9 条待办、4+ 好友关系已写入 Supabase
- 脚本：`scripts/qa/qa-smoke-v2.mjs`

## 23:15 — 多账号聊天冒烟测试（3 账号 36/36 全通过）

- 新建 `scripts/qa/chat-smoke.mjs`，覆盖 3 账号完整聊天链路
- 测试账号：A（现有）+ B（新建）+ C（新建）
- 聊天场景：A↔B（A 主动发 4 条）、C↔A（双向 5 条）、C↔B（独立对话 4 条）
- 好友链路：3 对好友关系全部建立（A↔B、A↔C、C↔B）
- API 验证：对话列出、消息数、消息内容、已读标记全部通过
- 浏览器验证：消息列表页、A↔B 对话详情、C↔A 对话详情均截图保存
- 修复：好友申请返回结构为 `{friend: {id}}` 而非 `requester_id`

## 22:52 — 项目文件整理清理

按照用户的清理清单，删除了以下未跟踪文件（保留 picture/、scripts/、qa/）：
- `h5/` — 独立 H5 页面，与项目无关
- `overview.md` — 未跟踪的设计总结文档
- `root/.DS_Store` — macOS 系统文件
- `frontend/.expo/web/cache/` — Expo web 缓存

## 23:00 — 同步更新项目核心文档

根据当前项目实际状态，同步更新了 5 个核心文档：

1. **PRD.md** — 重写：删除旧版本变更记录、旧路线规划和审计内容；保留当前功能定义、模块矩阵、交互/视觉规范、技术约束；缩减为 11 个章节
2. **database-init.sql** — 重写：合并所有 `DO $$ ALTER TABLE` 迁移语句到 `CREATE TABLE` 定义，按功能域分组（14 个表），保持幂等
3. **PROTOTYPE.md** — 重写：从 v1.3.1 更新到 v1.4.4，覆盖全部 19 个页面/模块的路由、布局、交互流和异常状态，新增数据模型关系图和验证命令
4. **TASKS.md** — 精简：删除所有 done 任务，仅保留 T80.3-T80.6 四个待办任务
5. **AGENTS.md** — 增强：新增文档优先级表、跨文档协作规则、AI 分工（代码/验证/文档维护）
